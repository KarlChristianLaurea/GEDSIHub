@page
@model GADApplication.Pages.GADO.MandatePage.IndexModel

@{
    ViewData["Title"] = "Index";
   
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Mandate[0].RepublicAct)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mandate[0].ChedMemo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mandate[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mandate[0].Activity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Mandate) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RepublicAct)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ChedMemo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Activity.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
