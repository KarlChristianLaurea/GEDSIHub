{
  "ConnectionStrings": {
    "DefaultConnection": "Server=${DB_SERVER}; Database=${DB_NAME}; User ID=${DB_USER}; Password=${DB_PASSWORD}; Connect Timeout=30; Encrypt=True; Trust Server Certificate=True; Application Intent=ReadWrite; Multi Subnet Failover=False",
    "IdentityConnection": "Server=${DB_SERVER}; Database=${DB_IDENTITY_DB}; User ID=${DB_USER}; Password=${DB_PASSWORD}; Connect Timeout=30; Encrypt=True; Trust Server Certificate=True; Application Intent=ReadWrite; Multi Subnet Failover=False"
  },
  "EmailSettings": {
    "SmtpServer": "${SMTP_SERVER}",
    "SmtpPort": "${SMTP_PORT}",
    "SmtpUsername": "${SMTP_USERNAME}",
    "SmtpPassword": "${SMTP_PASSWORD}",
    "SenderName": "${SENDER_NAME}",
    "SenderEmail": "${SENDER_EMAIL}",
    "UseSSL": true
  },
  "AllowedHosts": "*"
}
